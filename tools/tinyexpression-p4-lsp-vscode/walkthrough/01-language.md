# 小さな式から始めます

```tinyexpression
if($member & $score >= 80){'pass'}else{'retry'}
```

- `$name` はCalculationContextの変数です。
- 補完は構文候補とカタログ変数を提示します。
- 診断には安定した `TE*` / `FI*` エラーコードが付きます。

全構文を暗記する必要はありません。言語ガイドを横に開き、補完とエラー説明を使いながら進められます。
