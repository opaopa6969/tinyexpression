var $price1 as number set 11;
var $country1 as string set 'jp';
var $price2 as number set 12;
var $country2 as string set 'jp';
var $price3 as number set 13;
var $country3 as string set 'jp';
var $price4 as number set 14;
var $country4 as string set 'jp';
var $price5 as number set 15;
var $country5 as string set 'jp';
var $price6 as number set 16;
var $country6 as string set 'jp';
var $price7 as number set 17;
var $country7 as string set 'jp';
var $price8 as number set 18;
var $country8 as string set 'jp';
var $price9 as number set 19;
var $country9 as string set 'jp';
var $price10 as number set 20;
var $country10 as string set 'jp';
var $price11 as number set 21;
var $country11 as string set 'jp';
var $price12 as number set 22;
var $country12 as string set 'jp';
var $price13 as number set 23;
var $country13 as string set 'jp';
var $price14 as number set 24;
var $country14 as string set 'jp';
var $price15 as number set 25;
var $country15 as string set 'jp';
var $price16 as number set 26;
var $country16 as string set 'jp';
@bench(name='scaling')
if(not($price1 * 2 <= 10) & $country1.in('jp', 'us')) {
  max(pow($price1, 2), abs(-3)) + call bonus1($price1)
} else {
  match {
    $price1 > 100 -> 1,
    $price1 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price2 * 2 <= 10) & $country2.in('jp', 'us')) {
  max(pow($price2, 2), abs(-3)) + call bonus2($price2)
} else {
  match {
    $price2 > 100 -> 1,
    $price2 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price3 * 2 <= 10) & $country3.in('jp', 'us')) {
  max(pow($price3, 2), abs(-3)) + call bonus3($price3)
} else {
  match {
    $price3 > 100 -> 1,
    $price3 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price4 * 2 <= 10) & $country4.in('jp', 'us')) {
  max(pow($price4, 2), abs(-3)) + call bonus4($price4)
} else {
  match {
    $price4 > 100 -> 1,
    $price4 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price5 * 2 <= 10) & $country5.in('jp', 'us')) {
  max(pow($price5, 2), abs(-3)) + call bonus5($price5)
} else {
  match {
    $price5 > 100 -> 1,
    $price5 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price6 * 2 <= 10) & $country6.in('jp', 'us')) {
  max(pow($price6, 2), abs(-3)) + call bonus6($price6)
} else {
  match {
    $price6 > 100 -> 1,
    $price6 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price7 * 2 <= 10) & $country7.in('jp', 'us')) {
  max(pow($price7, 2), abs(-3)) + call bonus7($price7)
} else {
  match {
    $price7 > 100 -> 1,
    $price7 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price8 * 2 <= 10) & $country8.in('jp', 'us')) {
  max(pow($price8, 2), abs(-3)) + call bonus8($price8)
} else {
  match {
    $price8 > 100 -> 1,
    $price8 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price9 * 2 <= 10) & $country9.in('jp', 'us')) {
  max(pow($price9, 2), abs(-3)) + call bonus9($price9)
} else {
  match {
    $price9 > 100 -> 1,
    $price9 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price10 * 2 <= 10) & $country10.in('jp', 'us')) {
  max(pow($price10, 2), abs(-3)) + call bonus10($price10)
} else {
  match {
    $price10 > 100 -> 1,
    $price10 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price11 * 2 <= 10) & $country11.in('jp', 'us')) {
  max(pow($price11, 2), abs(-3)) + call bonus11($price11)
} else {
  match {
    $price11 > 100 -> 1,
    $price11 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price12 * 2 <= 10) & $country12.in('jp', 'us')) {
  max(pow($price12, 2), abs(-3)) + call bonus12($price12)
} else {
  match {
    $price12 > 100 -> 1,
    $price12 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price13 * 2 <= 10) & $country13.in('jp', 'us')) {
  max(pow($price13, 2), abs(-3)) + call bonus13($price13)
} else {
  match {
    $price13 > 100 -> 1,
    $price13 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price14 * 2 <= 10) & $country14.in('jp', 'us')) {
  max(pow($price14, 2), abs(-3)) + call bonus14($price14)
} else {
  match {
    $price14 > 100 -> 1,
    $price14 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price15 * 2 <= 10) & $country15.in('jp', 'us')) {
  max(pow($price15, 2), abs(-3)) + call bonus15($price15)
} else {
  match {
    $price15 > 100 -> 1,
    $price15 > 50 -> 2,
    default -> 0
  }
}
+ if(not($price16 * 2 <= 10) & $country16.in('jp', 'us')) {
  max(pow($price16, 2), abs(-3)) + call bonus16($price16)
} else {
  match {
    $price16 > 100 -> 1,
    $price16 > 50 -> 2,
    default -> 0
  }
}
float bonus1($x as number) {
  sqrt($x) + sin(30)
}
float bonus2($x as number) {
  sqrt($x) + sin(30)
}
float bonus3($x as number) {
  sqrt($x) + sin(30)
}
float bonus4($x as number) {
  sqrt($x) + sin(30)
}
float bonus5($x as number) {
  sqrt($x) + sin(30)
}
float bonus6($x as number) {
  sqrt($x) + sin(30)
}
float bonus7($x as number) {
  sqrt($x) + sin(30)
}
float bonus8($x as number) {
  sqrt($x) + sin(30)
}
float bonus9($x as number) {
  sqrt($x) + sin(30)
}
float bonus10($x as number) {
  sqrt($x) + sin(30)
}
float bonus11($x as number) {
  sqrt($x) + sin(30)
}
float bonus12($x as number) {
  sqrt($x) + sin(30)
}
float bonus13($x as number) {
  sqrt($x) + sin(30)
}
float bonus14($x as number) {
  sqrt($x) + sin(30)
}
float bonus15($x as number) {
  sqrt($x) + sin(30)
}
float bonus16($x as number) {
  sqrt($x) + sin(30)
}
